<p id="notice"><%= notice %></p>

<h1>Csr Performance Reports</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @csr_reports.each do |report| %>
      <tr>
        <td><%= report.csr_works_no %><td>
      </tr>
      <tr>
        <td><%= link_to 'Show', report %></td>
        <td><%= link_to 'Edit', edit_csr_performance_report_path(report) %></td>
        <td><%= link_to 'Destroy', report, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Csr Performance Report', new_csr_performance_report_path %>
